using System;

using SharpSploit.Enumeration;

public static class Task
{
    public static string Execute(string Path = "")
    {
        return Path == "" ? Host.GetDirectoryListing().ToString() : Host.GetDirectoryListing(Path).ToString();
    }
}